import React from "react";

export default class Projects extends React.Component {
    render() {
        return (
            <section className={"w-screen flex h-screen bg-gray-900"}>

            </section>
        )
    }
}